
;; tags for using the snippet in wiki
;; %Tag(wikiTalker)%
(defpackage turtles-lisp-pkg
  (:nicknames :lturtle)
  (:use :roslisp :cl :roslisp_tutorials_turtles-srv)
  (:export
   ;; application-specific
  ;  :turtle-circle))
  ;  :move-turtle))
   :turn-turtle))
;; %EndTag(wikiTalker)%
